require "rubygems"
require "rake"

require "sparkle_tools"

SparkleTools.new('<%= name %>') do |s|
  s.server     = '<%= urlname %>.com'
  s.base_url   = "http://#{s.server}"
  s.remote_dir = '/path/to/upload/root/of/app'
end

